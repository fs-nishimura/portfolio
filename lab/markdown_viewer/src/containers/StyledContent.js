import React from 'react'
import { connect } from 'react-redux'
import * as Config from '../config/config';

let StyledContent = ({date,todos,addToDo}) => {

  return (
    <div id="styled">
      aaa
    </div>
  )
}

export default StyledContent
